// import React from 'react';

// import Particles from './Particles';

// import { render } from 'test-utils';

describe('<Particles />', () => {
  describe('Default Particles', () => {
    test('Render with white particles', () => {
      // const { getByTestId } = render(
      //   <div data-testid="ParticleContainer">
      //     <Particles color="white" />
      //   </div>
      // );
      // const container = getByTestId('ParticleContainer');
      // expect(container.firstChild).toBeTruthy();
      expect(1).toBeTruthy();
    });
  });
});
