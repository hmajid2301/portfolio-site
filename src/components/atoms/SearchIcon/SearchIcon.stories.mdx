import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import SearchIcon from './SearchIcon';

<Meta title="SearchIcon" component={SearchIcon} />

# SearchIcon

<Preview>
  <Story
    name="Basic"
    args={{
      background: 'white',
      color: 'black',
      hoverColor: 'blue-500',
    }}
  >
    {(args) => <SearchIcon {...args} />}
  </Story>
</Preview>

<Props story="Basic" />

## Background

You can adjust the background colour of icon the by passing the `background` prop.

<Preview>
  <Story name="Background">
    <div>
      <SearchIcon background="gray-800" color="white" hoverColor="blue-500" />
      <SearchIcon background="indigo-500" color="white" hoverColor="blue-500" />
    </div>
  </Story>
</Preview>

## Color

You can adjust the colour if you pass the `color` prop.

<Preview>
  <Story name="Color">
    <div>
      <SearchIcon
        background="gray-500"
        color="blue-500"
        hoverColor="blue-500"
      />
      <SearchIcon
        background="gray-500"
        color="purple-500"
        hoverColor="blue-500"
      />
    </div>
  </Story>
</Preview>

## Hover Color

You can adjust the colour if you pass the `color` prop.

<Preview>
  <Story name="Hover Color">
    <div>
      <SearchIcon background="gray-500" color="blue-500" hoverColor="black" />
      <SearchIcon
        background="gray-500"
        color="purple-500"
        hoverColor="green-500"
      />
    </div>
  </Story>
</Preview>
