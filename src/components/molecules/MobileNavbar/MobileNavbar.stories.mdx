import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { INITIAL_VIEWPORTS } from '@storybook/addon-viewport';

import { theme } from '~/styles';

import MobileNavbar from './MobileNavbar';

<Meta
  title="Molecules | MobileNavbar"
  component={MobileNavbar}
  parameters={{
    viewport: {
      viewports: INITIAL_VIEWPORTS,
      defaultViewport: 'iphone6',
    },
  }}
/>

# MobileNavbar

<Preview>
  <Story
    name="Basic"
    args={{
      background: 'gray-800',
      color: 'black',
      hoverColor: 'blue-500',
      links: ['Home', 'Blog', 'Projects', 'Open Source'],
    }}
  >
    {(args) => <MobileNavbar {...args} />}
  </Story>
</Preview>

<Props story="Basic" />

## Background

You can adjust the background colour of the header by passing the `background` prop.

<Preview>
  <Story name="Background">
    <MobileNavbar
      background="white"
      color="black"
      hoverColor="blue-500"
      links={['Home', 'Blog', 'Projects', 'Open Source']}
    />
    >
  </Story>
</Preview>

## Color

You can change the link colour by passing the `color` prop.

<Preview>
  <Story name="Color">
    <MobileNavbar
      background="white"
      color="black"
      hoverColor="blue-500"
      links={['Home', 'Blog', 'Projects', 'Open Source']}
    />
  </Story>
</Preview>

## Hover Color

You can change the hover colour of the link text by passing the `hoverColor` prop.

<Preview>
  <Story name="Hover Color">
    <MobileNavbar
      background="blue-500"
      color="orange-500"
      hoverColor="purple-500"
      links={['Home', 'Blog', 'Projects', 'Open Source']}
    />
  </Story>
</Preview>

## Links

You can change the links shown by passing the `link` prop.

<Preview>
  <Story name="Links">
    <MobileNavbar
      background="blue-500"
      color="orange-500"
      hoverColor="purple-500"
      links={['Home', 'Main Projects', 'What I Use?', 'FAQ']}
    />
  </Story>
</Preview>

## Dark Mode

Example dark mode component.

<Preview>
  <Story name="Dark Mode">
    <MobileNavbar
      background="gray-800"
      color={theme.dark.text}
      hoverColor="purple-500"
      links={['Home', 'Main Projects', 'What I Use?', 'FAQ']}
    />
  </Story>
</Preview>
