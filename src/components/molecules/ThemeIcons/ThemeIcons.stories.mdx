import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import ThemeIcons from './ThemeIcons';

<Meta title="Molecules | ThemeIcons" component={ThemeIcons} />

# ThemeIcons

<Preview>
  <Story name="Basic" args={{}}>
    {(args) => <ThemeIcons {...args} />}
  </Story>
</Preview>

<Props story="Basic" />

## OnClick

You can define the function to be called when the icons are pressed by passing `onClick` prop.

<Preview>
  <Story name="onClick">
    <ThemeIcons onClick={() => console.log('Pressed Icon')} />
  </Story>
</Preview>
