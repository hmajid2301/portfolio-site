import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import {
  AiFillGithub as Github,
  AiFillGitlab as Gitlab,
  AiFillMediumCircle as Medium,
} from 'react-icons/ai';
import { FaDev as Dev } from 'react-icons/fa';

import { theme } from '~/styles';
import Footer from './Footer';

<Meta title="Organisms | Footer" component={Footer} />

# Footer

<Preview>
  <Story name="Basic" args={{}}>
    {(args) => <Footer {...args} />}
  </Story>
</Preview>

<Props story="Basic" />

## Background

You can adjust the background colour of the footer by passing the `background` prop.

<Preview>
  <Story name="Background">
    <Footer background="white" />
  </Story>
</Preview>

## Hover Color

You can change the hover colour of the link text by passing the `hoverColor` prop.

<Preview>
  <Story name="Hover Colour">
    <Footer hoverColor="purple-500" />
  </Story>
</Preview>

## Color

You can colour of the text `color` prop.

<Preview>
  <Story name="Colour">
    <Footer color="green-700" />
  </Story>
</Preview>

## Dark Mode

Example dark mode component.

<Preview>
  <Story name="Dark Mode">
    <div class={`bg-${theme.dark.background}`}>
      <Footer color={theme.dark.text} background="gray-800" />
    </div>
  </Story>
</Preview>
