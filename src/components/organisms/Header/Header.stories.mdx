import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import ThemeProvider from '~/providers/Theme';
import Header from './Header';

<Meta title="Organisms | Header" component={Header} />

# Header

<Preview>
  <Story
    name="Basic"
    args={{
      links: [
        { name: 'Home', link: '/' },
        { name: 'Blog', link: '/blog' },
        { name: 'Tags', link: '/tag' },
        { name: 'Uses', link: '/uses' },
      ],
    }}
  >
    {(args) => (
      <ThemeProvider>
        <Header {...args} />
      </ThemeProvider>
    )}
  </Story>
</Preview>

<Props story="Basic" />

## Links

You can adjust the links shown by passing `link` prop.

<Preview>
  <Story name="Links">
    <ThemeProvider>
      <Header
        links={[
          { name: '🏠️ Home', link: '/' },
          { name: '✍️ Blog', link: '/blog/' },
          { name: '🧮️ Stats', link: '/stats/' },
          { name: '📋 Uses', link: '/uses/' },
        ]}
      />
    </ThemeProvider>
  </Story>
</Preview>
